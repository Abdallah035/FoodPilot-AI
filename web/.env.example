# Base URL of the FastAPI bridge (server/app.py). Used by next.config rewrites.
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Set to "true" to use the built-in mock agent flow instead of the live backend.
# Great for design work and demos with no backend running.
NEXT_PUBLIC_USE_MOCK=true
