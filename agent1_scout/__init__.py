"""Food Pilot — Agent 1: Discovery / Scout (Finder + Picker).

A LangGraph agent that turns a vague craving into a concrete selected
restaurant and a specific menu deal, then outputs a structured JSON payload.
"""

__version__ = "0.1.0"
